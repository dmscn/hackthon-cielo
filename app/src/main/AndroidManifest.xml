<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.damasceno.hackthon_cielo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_large"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_large"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddCheck" />
        <activity android:name=".PontosActivity" />
        <activity android:name=".CadActivity" />
        <activity android:name=".ConsultaActivity" />
        <activity android:name=".CartaoActivity"></activity>
    </application>

</manifest>